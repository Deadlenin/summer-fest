spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: localhost
    port: 25
    username: test
    password: test

app:
  admin:
    username: admin
    password: admin
  notification:
    enabled: false
    from: test@localhost
  gallery:
    storage-path: ${java.io.tmpdir}/eventplatform-gallery-test
  cors:
    allowed-origins: http://localhost:5173
